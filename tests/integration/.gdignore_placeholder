# Integration tests go here — one subdirectory per system.
# Save/load round-trips live under tests/integration/save_load/.
# Cross-system tests (e.g., Economy × Orchestrator) live under tests/integration/[primary-system]/.
