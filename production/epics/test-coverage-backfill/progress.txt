# Test Coverage Backfill — Ralph Progress Log

Append-only log per EPIC.md FR-9. Format:
  <US-NNN> <basename> | <passes_status> | <new_tests_added> | <notes>

## Codebase Patterns

- Test runner invocation (macOS, Godot mono build):
    /Applications/Godot_mono.app/Contents/MacOS/Godot --headless \
      -s addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode \
      -a tests/unit/<dir-or-file>
  The wrapper `tests/gdunit4_runner.gd` exists but the direct CmdTool form is
  what CI uses. `--ignoreHeadlessMode` is REQUIRED — without it the runner
  exits 103 (headless-mode unsupported guard).
- Audit predicate (per EPIC technical considerations):
    grep -nE "^func [a-z]" src/core/<system>/<file>.gd   # public funcs
    grep -rn "<func_name>" tests/                         # references
- Pre-existing baseline failures observed (NOT regressions, do not let them
  block US-NNN closure if your file is unaffected):
    * tests/unit/audio_router/audio_router_signal_handlers_test.gd ::
      test_prestige_completed_handler_fires_play_sfx_with_volume_1_point_2
      (2 fail count — single test, recorded twice; investigation deferred,
      not in scope for test-coverage-backfill epic per FR-8 reading).
- Test naming convention in this directory permits both `test_<system>_<...>`
  and `test_<func>_<scenario>_<expected>`. See
  tests/unit/audio_router/volume_persistence_round_trip_test.gd for the
  function-prefixed style which matches stop_music_test.gd.

---

## Iteration log

## 2026-05-10 — US-001 audio_router.gd

- Audited 13 public funcs (set/get_master/music/sfx_volume_db,
  set_master_muted, is_master_muted, play_sfx, play_music, stop_music,
  get_save_data, load_save_data). All 13 had ≥1 test reference.
- Gap found: stop_music had only `does_not_crash` smoke check; no assertion
  on documented side effects (clearing _current_ambient_player /
  _current_ambient_id; no-op when no bed playing).
- Added tests/unit/audio_router/stop_music_test.gd with 3 tests:
    * test_stop_music_after_play_music_clears_current_ambient_id (happy)
    * test_stop_music_after_play_music_clears_current_ambient_player (happy)
    * test_stop_music_with_no_bed_playing_leaves_state_clean (edge)
- Test result: 3/3 passed; full audio_router/ suite shows 1 pre-existing
  failure (test_prestige_completed_handler_*) confirmed to exist on baseline
  (verified via git stash). No new failures introduced.
- Files changed: tests/unit/audio_router/stop_music_test.gd (new),
  production/epics/test-coverage-backfill/progress.txt (new).
- Status: US-001 audio_router | passes:true | new_tests_added:3 | notes:filled stop_music side-effect coverage gap; volume getters/setters/muted/play_sfx/play_music/save_load already covered.

**Learnings for future iterations:**
- The `_get_ar()` accessor + `_reset_ambient_state()` hygiene barrier in
  stop_music_test.gd is a reusable pattern for audio_router-touching tests.
- Run a single test file FIRST (`-a tests/unit/<dir>/<file>_test.gd`) to
  validate green, then run the directory for regression check. If the
  directory shows failures absent from your single-file run, those are
  pre-existing — confirm via `git stash -u && rerun && stash pop`.
- The pre-existing prestige-completed test failure does NOT affect any
  US-NNN whose subject file isn't audio_router signal_handlers internals.
  US-001 closes as-is; flag for separate triage outside this epic.

---

## 2026-05-10 — US-002 data_registry.gd

- Audited 4 public funcs (resolve, get_all_by_type, hot_reload, verify_integrity).
  All 4 have >=1 happy-path + >=1 edge-case test with assert_* calls.
- Coverage map:
    * resolve: tests/unit/data_registry/resolve_api_and_typed_accessors_test.gd
        - test_resolve_valid_id_returns_cached_resource_and_identity_equality (happy)
        - test_resolve_missing_id_returns_null_with_warn_behavior (edge)
        - test_resolve_unknown_content_type_returns_null_without_crash (edge)
        - test_resolve_before_registry_ready_returns_null (edge)
        - test_resolve_assert_behavior_returns_null_after_assert_fires (edge)
        - test_resolve_rename_transparent_id_based_lookup (edge)
        - tests/unit/data_registry/cross_type_dag_and_invariants_test.gd::test_ac_dls_06_post_error_resolve_returns_null_for_cyclic_resources (POST_ERROR edge)
    * get_all_by_type: same resolve_api file
        - test_get_all_by_type_returns_full_array_with_identity_equality (happy + unknown-category sub-assert)
        - test_get_all_by_type_before_registry_ready_returns_empty_array (edge)
    * hot_reload:
        - tests/unit/data_registry/autoload_skeleton_and_state_machine_test.gd::test_data_registry_hot_reload_complete_signal_has_correct_arity_and_type (signal contract)
        - tests/integration/data_registry/hot_reload_immutability_and_hydration_gate_test.gd::test_hot_reload_re_enumerates_target_category_only_and_emits_signal (happy + scope-isolation edge)
    * verify_integrity:
        - tests/integration/data_registry/hot_reload_immutability_and_hydration_gate_test.gd
          (baseline returns no mismatches = happy; consumer-mutated resource surfaces mismatches = edge)
- Test result: tests/unit/data_registry suite green — 41 cases / 0 errors / 0 failures / 337ms.
- New tests added: 0 (FR-6: existing coverage at-or-above bar).
- Files changed: production/epics/test-coverage-backfill/progress.txt only.
- Status: US-002 data_registry | passes:true | new_tests_added:0 | notes:audit-only; full happy+edge coverage already shipped via Story 004 resolve_api suite + hot-reload integration suite.

**Learnings for future iterations:**
- The `hot_reload` + `verify_integrity` happy-path coverage lives under
  tests/integration/data_registry/, NOT tests/unit/. Per FR-2/FR-3 the audit
  predicate ("function appears in test code with an assert_* call within ~5
  lines") is satisfied at integration tier, because the epic's goal is
  *behavioral coverage exists*, not *coverage tier matches story tier*.
  Future audits should grep tests/ (not tests/unit/) and accept either tier.
- `cross_type_dag_and_invariants_test.gd::test_ac_dls_06_post_error_resolve_returns_null_for_cyclic_resources`
  is a load-bearing edge case for `resolve()` under POST_ERROR state — useful
  precedent when auditing other autoload state-machine error paths (TickSystem,
  SaveLoadSystem) in later iterations.

---

## 2026-05-10 — US-003 dungeon_run_orchestrator.gd

- Audited 17 public funcs: report_offline_replay_error,
  mark_floor_invalid_for_offline_replay, set_combat_resolver,
  set_matchup_resolver, set_error_logger, set_floor_unlock, dispatch,
  attribute_kill_gold, attribute_kill_xp, flush_offline_signals,
  compute_offline_total_xp, xp_per_kill, xp_per_floor_clear,
  get_last_dispatch_intent, resolve_floor_by_snapshot_id, get_save_data,
  load_save_data. All 17 have >=1 happy-path + >=1 edge-case test with
  assert_* calls.
- Coverage map (per-function reference test):
    * report_offline_replay_error / mark_floor_invalid_for_offline_replay:
      tests/integration/dungeon_run_orchestrator/offline_error_and_floor_validity_test.gd
      (TR-029/TR-031 happy + zero-partial-gold + null-snapshot edge cases).
    * set_combat/matchup/error_resolver, set_floor_unlock: autoload_skeleton_and_di_test.gd
      (replaces_internal_field happy + survives_ready_call edge — DI invariant).
    * dispatch: 42 test files; deeply covered.
    * attribute_kill_gold/xp: tests/unit/dungeon_run_orchestrator/class_synergy_formula_test.gd
      (steel_wall vs bruiser/skirmisher, triple_threat unconditional, arcane_elite XP,
      no_synergy baseline — 11 assertion-bearing tests).
    * flush_offline_signals: 8 files (replay-completion side-effects covered).
    * compute_offline_total_xp / xp_per_kill / xp_per_floor_clear:
      offline_xp_batch_test.gd + stub_xp_grant_test.gd (tier-scaling happy paths +
      clamp-to-zero edge for negative/zero floor index).
    * get_last_dispatch_intent: last_dispatch_intent_test.gd
      (initial-empty + after-success-populated + deep-copy-isolation).
    * resolve_floor_by_snapshot_id: snapshot_and_matchup_cache_test.gd
      (returns_null_for_unknown_id + returns_null_for_empty_id — covers BOTH
      branches of current implementation; "floors" is NOT in
      DataRegistry.ORDERED_CATEGORIES so non-null returns are not currently
      reachable, leaving the empty-id and unknown-id null returns as the only
      observable behaviors).
    * get_save_data / load_save_data: 16/18 files (round-trip + corrupt-payload
      coverage via save_load_system + autoload_persist suites).
- Test result: tests/unit/dungeon_run_orchestrator suite green —
  209 cases / 0 errors / 0 failures / 3s 21ms.
- New tests added: 0 (FR-6: existing coverage at-or-above bar).
- Files changed: production/epics/test-coverage-backfill/progress.txt only.
- Status: US-003 dungeon_run_orchestrator | passes:true | new_tests_added:0 | notes:audit-only; orchestrator is heaviest-tested file in project (42 dispatch-touching test files). All 17 public funcs covered with happy+edge across unit + integration tiers.

**Learnings for future iterations:**
- For accessor-style functions (`set_*` DI seams) the canonical happy+edge pair
  is "replaces_internal_field" (happy) + "survives_ready_call" (edge —
  injection-before-_ready survives the lazy-default branch). When auditing
  other autoloads' DI seams (TickSystem, SaveLoadSystem, SceneManager), look
  for this paired pattern in autoload_skeleton_*.gd files and accept it as
  full coverage.
- For functions with implementation branches gated by data-registry state
  (e.g., `resolve_floor_by_snapshot_id` only returns non-null if "floors" were
  a registered category — it isn't), BOTH observable-behavior branches (empty
  + unknown) ARE the full coverage surface. A "happy path" doesn't have to
  return a non-null/non-error value; it just has to assert the documented
  return shape. This is consistent with FR-2 ("documented return shape /
  behavior").
- Heavy-coverage files like dungeon_run_orchestrator (42 test files) only
  need an audit pass — searching `grep -rn "<func_name>" tests/` and
  spot-checking the assertion shape suffices. Don't be tempted to "improve"
  test names or refactor existing tests; that's out of scope per FR-8 spirit
  ("non-regression in baseline" implies "don't touch passing tests").

---

## 2026-05-10 — US-004 economy.gd

- Audited 14 public funcs: add_gold, try_spend, try_award_floor_clear,
  compute_offline_batch, set_offline_replay_inputs, flush_offline_signals,
  get_save_data, load_save_data, get_gold_balance, get_lifetime_gold_earned,
  is_first_clear_awarded, get_config, recruit_cost, level_cost.
- Coverage map (per-function):
    * add_gold: economy_add_gold_test.gd (happy + clamp edge + zero/negative-amount edges)
    * try_spend: economy_try_spend_test.gd (sufficient-balance happy + zero/negative/insufficient edges)
    * try_award_floor_clear: economy_try_award_floor_clear_test.gd (first-clear happy + monotonic-credit + repeat-WIN/LOSING-after-WIN edges)
    * compute_offline_batch + set_offline_replay_inputs:
      tests/integration/economy/economy_offline_batch_determinism_test.gd
      (closed-form drip happy + 100-iteration determinism + zero-tick + negative-tick + 1M-tick no-overflow edges).
      set_offline_replay_inputs is exercised in EVERY happy/edge test of compute_offline_batch as the input-injection seam — covered transitively.
    * flush_offline_signals: tests/unit/economy/flush_offline_signals_test.gd (per-replay flush happy + double-flush + no-pending-state edges)
    * get_save_data / load_save_data: tests/integration/economy/economy_save_load_round_trip_test.gd (round-trip happy + schema_version_mismatch edge + malformed-payload edge)
    * get_gold_balance / get_lifetime_gold_earned: 14 / 6 references each (happy via post-add_gold + post-try_spend; edge via fresh-instance == 0)
    * is_first_clear_awarded: economy_autoload_skeleton_test.gd::test_economy_is_first_clear_awarded_returns_false_for_uncredited_floor (uncredited-floor edge); credited-floor happy via try_award_floor_clear_test sub-asserts.
    * recruit_cost / level_cost: economy_recruit_cost_test.gd + economy_level_cost_test.gd (tier-1 happy at multiple copies_owned + sentinel paths: negative, orphan class_id, null _config, tier-not-in-BASE)
    * get_config: GAP — no direct test until this iteration. Other funcs hit `_config` indirectly but the accessor itself had no happy + null-state coverage.
- Gap closed: tests/unit/economy/economy_get_config_test.gd (6 new tests):
    * test_get_config_after_ready_returns_registered_economy_config_instance (happy)
    * test_get_config_returns_same_instance_on_repeat_calls (identity invariant)
    * test_get_config_before_ready_returns_null_without_raising (pre-_ready edge)
    * test_get_config_after_explicit_null_override_returns_null (DataRegistry-miss edge)
    * test_get_config_does_not_mutate_gold_balance_or_lifetime (pure-accessor invariant)
    * test_get_config_returns_handed_seeded_config_via_field_override (DI-seed pattern)
- Test result: tests/unit/economy suite green —
  132 cases / 0 errors / 0 failures / 1s 92ms (was 126; +6 from new file).
- New tests added: 6.
- Files changed: tests/unit/economy/economy_get_config_test.gd (new),
  production/epics/test-coverage-backfill/progress.txt.
- Status: US-004 economy | passes:true | new_tests_added:6 | notes:closed get_config() accessor gap; all other 13 public funcs already at-or-above bar via existing economy/ unit + integration suites.

**Learnings for future iterations:**
- For trivial accessor funcs (`get_config`, future `get_*` getters) the
  canonical happy+edge surface is: live-autoload happy (returns registered
  instance with populated content) + pre-_ready null edge (bare .new()) +
  explicit-null edge (post-_ready field reset, simulating registry-miss
  branch) + identity invariant (repeat calls return same instance) + pure-
  accessor invariant (no state mutation). Mirrors the
  `recruit_cost_null_config_returns_minus_one` add_child + null-override
  pattern from economy_recruit_cost_test.gd.
- Audit predicate refinement: when a public func has 0 direct test
  references (`grep -rn "<func_name>" tests/` returns nothing matching the
  call site), it's an unambiguous gap regardless of how heavily its return
  value is consumed elsewhere. The existing economy/ suite extensively
  exercised `_config` via recruit_cost/level_cost/compute_offline_batch but
  never `economy.get_config()` itself — pure accessor public surface still
  needs at least one direct call site under assertion.
- `set_offline_replay_inputs` is a test-only DI seam (per its docstring
  "Production callers MUST NOT set this"); coverage via every
  compute_offline_batch happy test counts because the seam IS the input
  channel for those tests. Don't add a separate dedicated test for DI seams
  whose only legitimate caller is the test harness itself.

---

## 2026-05-10 — US-005 enemy_database.gd

- Audited 2 public funcs: get_by_id, get_all_ids. Both have >=1 happy-path
  + >=1 edge-case test with assert_* calls.
- Coverage map (per-function):
    * get_by_id:
      tests/integration/enemy_database/enemy_database_autoload_test.gd
        - test_enemy_database_get_by_id_returns_null_on_miss (edge: missing id, TR-enemy-db-022)
        - test_enemy_database_get_by_id_empty_string_returns_null (edge: empty string)
        - test_enemy_database_get_by_id_resolves_mvp_enemies (happy: id round-trip + tier/display_name invariants, AC H-01)
        - test_enemy_database_get_by_id_returns_same_instance (happy: object-identity / cache consistency)
    * get_all_ids:
      tests/integration/enemy_database/enemy_database_autoload_test.gd
        - test_enemy_database_get_all_ids_returns_sorted_array (happy: sorted alphabetically + size>=7 when MVP loaded; edge: empty when DataRegistry not READY)
        - test_enemy_database_get_by_id_resolves_mvp_enemies (transitively asserts get_all_ids returns iterable Array[String])
- Test result: tests/integration/enemy_database suite green —
  7 cases / 0 errors / 0 failures / 41ms.
- New tests added: 0 (FR-6: existing coverage at-or-above bar).
- Files changed: production/epics/test-coverage-backfill/progress.txt only.
- Status: US-005 enemy_database | passes:true | new_tests_added:0 | notes:audit-only; both public funcs covered at integration tier per US-002 precedent. Coverage tier acceptable since EPIC.md FR-2 specifies *behavioral coverage exists*, not tier-must-match-story.

**Learnings for future iterations:**
- The graceful-degrade pattern in enemy_database_autoload_test.gd
  (push_warning + return when DataRegistry is not READY) is correct for
  integration tests but produces tests that *trivially pass* without
  assertions in environments where the DataRegistry has 0 enemy .tres
  files. For this epic's audit predicate ("happy + edge with assert_*
  within 5 lines"), the asserted branches (READY-state + sorted + tier
  invariants + identity) ARE the happy/edge surface. The push_warning
  branches are documented graceful-degrade fallbacks and don't need
  separate happy-path coverage.
- Thin typed-accessor wrappers (`get_by_id` = single-line DataRegistry
  resolve cast; `get_all_ids` = filter-and-sort over get_all_by_type) are
  a recurring pattern across rank-5 autoloads (EnemyDatabase,
  HeroClassDatabase, BiomeDungeonDatabase). Their coverage bar is met by
  asserting the *cast/filter behavior* (returns null/empty on miss; returns
  typed result + sort order on hit), not by re-asserting DataRegistry's
  own contracts.

---

## 2026-05-10 — US-006 floor_unlock_system.gd

- Audited 13 public funcs: set_warning_logger, set_error_logger, is_unlocked,
  is_unlocked_in_biome, is_biome_available, is_biome_completed,
  get_available_biomes, get_highest_cleared, get_floor_state, get_save_data,
  load_save_data, debug_set_highest_cleared, debug_reset.
- Coverage map (per-function):
    * set_warning_logger / set_error_logger: biome_floor_count_and_di_loggers_test.gd
      (TR-021 setter-replaces-default + injected-routing happy + fresh-instance
      default-invokable edge — 4 explicit DI-setter tests).
    * is_unlocked: floor_unlock_system_test.gd Group E
      (accessible/locked/sentinel/cleared — 4 tests covering true+false+R10).
    * is_unlocked_in_biome: floor_unlock_system_test.gd Group E2
      (matches-1-arg-form happy + unavailable-biome edge + R10/over-range edge).
    * is_biome_available / is_biome_completed / get_available_biomes:
      biome_availability_and_completeness_test.gd (TR-023/TR-024 — active/empty/
      unknown/multi-biome happy + fictional/empty-string/floor-count-zero/
      below-count/stale-biome edges).
    * get_highest_cleared: floor_unlock_system_test.gd Group C (fresh-save
      default = 0 happy + planned-biome-default = 0 edge); also exercised as
      assertion target across signal-handler/save-load/debug groups.
    * get_floor_state: floor_unlock_system_test.gd Group D (UNAVAILABLE/LOCKED/
      ACCESSIBLE/CLEARED matrix, 6 tests covering all 4 states + R10 sentinel +
      over-range + below-highest + at-frontier branches).
    * get_save_data: floor_unlock_system_test.gd Group G (canonical-schema
      happy + payload-independence edge — guards external-mutation leak).
    * load_save_data: floor_unlock_system_test.gd Group G + offline_replay_parity
      integration suite (round-trip happy + missing-key/under-range/over-range/
      non-numeric-tampered/lossy-float edges — 6 tests covering GDD §E
      6-step processing order).
    * debug_set_highest_cleared: GAP — only `has_method` existence test +
      consumed by victory_moment integration suite as setup. No direct
      happy/edge coverage.
    * debug_reset: GAP — only `has_method` existence test. Zero behavioral
      coverage despite being mutator (clear + reseed).
- Gap closed: tests/unit/floor_unlock_system/debug_helpers_test.gd (7 new tests):
    * test_debug_set_highest_cleared_writes_value_to_unlock_state (happy)
    * test_debug_set_highest_cleared_overwrites_existing_value (edge: bypasses
      monotone R4 invariant — debug helper is raw setter, can decrease)
    * test_debug_set_highest_cleared_accepts_unknown_biome_and_creates_entry
      (edge: no biome-availability validation on debug write — UI consumers
      filter via is_biome_available)
    * test_debug_set_highest_cleared_accepts_value_above_floor_count
      (edge: bypasses load_save_data's over-range clamp)
    * test_debug_reset_clears_existing_state_and_seeds_fresh_save_default (happy)
    * test_debug_reset_on_already_empty_state_seeds_fresh_save_default
      (edge: idempotent re-reset)
    * test_debug_reset_drops_stale_biome_entries (edge: removed-biome cleanup)
- Test result: tests/unit/floor_unlock_system suite green —
  73 cases / 0 errors / 0 failures / 1s 1ms (was 66; +7 from new file).
- New tests added: 7.
- Files changed: tests/unit/floor_unlock_system/debug_helpers_test.gd (new),
  production/epics/test-coverage-backfill/progress.txt.
- Status: US-006 floor_unlock_system | passes:true | new_tests_added:7 | notes:closed debug_set_highest_cleared + debug_reset behavioral-coverage gaps; all other 11 public funcs already at-or-above bar via existing floor_unlock_system suites.

**Learnings for future iterations:**
- The `OS.is_debug_build()` guard branch on debug helpers is NOT directly
  testable in gdunit4 (the test runner IS a debug build by definition; no
  hook lets us simulate `OS.is_debug_build() == false` without source-level
  refactor). Document the production-blocked branch as covered-by-construction
  and focus tests on the in-debug-build mutating path.
- When auditing autoloads with debug/test API surface (`debug_*` methods),
  expect them to be consumed as integration-test setup but NOT to have direct
  unit-test coverage. Future US-NNN audits for autoloads with debug helpers
  (HeroRoster.debug_*, FormationAssignment.debug_*, Recruitment.debug_*) will
  likely surface the same gap shape — reuse the helper-test file pattern from
  debug_helpers_test.gd.
- Mutator helpers (debug_*) need **paired happy + bypass-edge** tests:
  happy = "writes value, accessor reflects" (the contract);
  edge1 = "bypasses an invariant the production path enforces" (e.g., monotone
    R4 → debug can decrease; over-range clamp → debug accepts 999 unclamped);
  edge2 = "skips a validation the production path requires" (e.g., biome
    availability check → debug accepts unknown biome).
  This shape captures WHY the helper is dangerous in production code-paths.

---

## 2026-05-10 — US-007 formation_assignment.gd

- Audited 8 public funcs: browse, commit, get_save_data, load_save_data,
  set_target, get_target, detect_active_synergy, notify_synergy_detected.
- Coverage map (per-function):
    * browse: formation_assignment_skeleton_test.gd Group C
      (test_browse_emits_browse_opened_signal_with_payload happy +
      test_browse_is_idempotent_two_calls_emit_twice edge).
    * commit: GAP — only `has_method` existence check + a static-grep
      single-writer enforcement test (recruitment_single_writer_ci_grep_test.gd).
      Zero behavioral coverage of write-then-emit ordering, length-validation
      reject branch, or null-slot semantics. The skeleton test's top-level
      docstring listed Groups D ("commit() emits AFTER writes"), E ("commit()
      length validation"), F ("empty/null formation handling") but none of
      those test functions were ever written.
    * get_save_data: formation_assignment_skeleton_test.gd Group G
      (test_get_save_data_returns_empty_dict_per_mvp_rule_10_deferral happy);
      no edge cases — the function literally returns {} unconditionally per
      §C.6 MVP Rule 10 deferral; matchup_target_test.gd::
      test_formation_assignment_get_save_data_does_not_persist_matchup_target
      provides session-only-state invariant edge.
    * load_save_data: formation_assignment_skeleton_test.gd Group G
      (test_load_save_data_with_empty_dict_is_noop happy +
      test_load_save_data_with_non_empty_dict_is_noop_per_v1_0_reservation
      edge); plus matchup_target_test.gd::
      test_formation_assignment_load_save_data_does_not_clear_matchup_target
      session-state-invariant edge.
    * set_target: matchup_target_test.gd Groups B+C+D
      (captures_biome_and_floor happy + rejects_empty_biome_id /
      rejects_zero_floor_index / rejects_negative_floor_index defensive
      edges + overwrites_prior_target last-write-wins).
    * get_target: matchup_target_test.gd Groups A+B+E
      (initial_returns_empty_dict happy + after_set_returns_populated_dict
      happy + returns_deep_copy mutation-isolation edge).
    * detect_active_synergy: class_synergy_detection_test.gd
      (steel_wall/arcane_elite/triple_threat happy paths + 2W+1M no-synergy
      + missing-class_id + 2-slot under-size + heroes-path-vs-instance_ids
      branch coverage).
    * notify_synergy_detected: tests/integration/audio_router/
      class_synergy_audio_test.gd Group B (steel_wall/arcane_elite/
      triple_threat emit happy + empty-string no-emit edge). Coverage tier
      acceptable per US-002 precedent (FR-2 specifies behavioral coverage
      exists, not tier-must-match-story).
- Gap closed: tests/unit/formation_assignment/commit_test.gd (9 new tests):
    * test_commit_with_three_heroes_writes_all_formation_slots (happy)
    * test_commit_with_three_heroes_emits_reassignment_committed_signal (happy)
    * test_commit_with_short_formation_does_not_emit_signal
      (edge: length-mismatch reject branch)
    * test_commit_with_short_formation_does_not_mutate_formation_slots
      (edge: length-validation gate runs BEFORE write loop per §D)
    * test_commit_with_oversized_formation_does_not_emit_signal
      (edge: 4-element-formation reject)
    * test_commit_with_all_null_formation_clears_all_slots
      (edge: null entries → hero_id=0 sentinel write)
    * test_commit_with_partial_null_formation_writes_real_and_clears_null
      (edge: mixed real+null slot writes)
    * test_commit_with_all_null_formation_still_emits_signal
      (edge: empty-slot commit is a valid commit; signal fires)
    * test_commit_emits_signal_after_writes_complete
      (§D ordering invariant: subscriber sees post-mutation _formation_slots)
- Test result: tests/unit/formation_assignment suite green —
  60 cases / 0 errors / 0 failures / 810ms (was 51; +9 from new file).
- New tests added: 9.
- Files changed: tests/unit/formation_assignment/commit_test.gd (new),
  production/epics/test-coverage-backfill/progress.txt.
- Status: US-007 formation_assignment | passes:true | new_tests_added:9 | notes:closed commit() write-then-emit ordering + length-validation reject + null-slot-semantics gaps; all other 7 public funcs already at-or-above bar via existing skeleton/matchup_target/class_synergy_detection suites.

**Learnings for future iterations:**
- The "skeleton test docstring promises Groups X/Y/Z but only Group A is
  actually written" anti-pattern recurs. When auditing autoloads with
  skeleton_test.gd files, do NOT trust the file's top-level Test Groups
  comment — grep for the actual `func test_*` functions and confirm each
  promised group has at least one assertion-bearing test. The `commit()`
  gap was hiding behind a skeleton-test docstring that listed Groups D/E/F
  but none of those tests existed.
- For autoloads whose public methods mutate ANOTHER live autoload (here:
  FormationAssignment.commit → HeroRoster._formation_slots), the canonical
  test isolation pattern is:
    1. Snapshot the target autoload's mutable field in before_test.
    2. Inject synthetic state via `<Autoload>._private[id] = fake +
       _injected_ids.append(id)`.
    3. Restore snapshot + erase injected entries in after_test.
  Mirrors prestige_button_visibility_test.gd:30 hero injection pattern but
  generalised to formation slots. Subsequent US-NNN audits for
  cross-autoload-mutation methods (Recruitment.recruit → HeroRoster, etc.)
  should reuse this snapshot+inject+restore shape.
- Signal-after-mutation invariants (§D ordering) are testable with a
  capture-state-at-emit-time spy: subscribe a handler that copies the
  target autoload's mutable field into a local Array; assert the local
  Array reflects post-mutation state. This is more direct than relying on
  signal-emission-count alone, which can't distinguish "emitted before
  writes" from "emitted after writes".
- Length-mismatch reject branches typically have TWO observable
  consequences: (a) signal NOT emitted, (b) state NOT mutated. Test BOTH
  separately — a single combined test conflates the two failure modes
  (a code path that emits-then-bails would fail (a) but pass a
  state-only assertion).

---

## 2026-05-10 — US-008 hero_class_database.gd

- Audited 3 public funcs: get_by_id, get_recruitable_classes, get_all_ids.
  All 3 have >=1 happy-path + >=1 edge-case (or single-branch full surface)
  test with assert_* calls.
- Coverage map (per-function):
    * get_by_id:
      tests/integration/hero_class_database/hero_class_database_autoload_test.gd
        - test_hero_class_database_get_by_id_returns_null_on_miss
          (edge: missing id, TR-hero-class-db-014)
        - test_hero_class_database_get_by_id_empty_string_returns_null
          (edge: empty string)
        - test_hero_class_database_get_by_id_resolves_mvp_classes
          (happy: id round-trip + tier=1 + non-empty display_name, AC H-01)
        - test_hero_class_database_get_by_id_returns_same_instance
          (happy: object-identity / Godot Resource cache invariant, AC H-07)
    * get_recruitable_classes:
      tests/integration/hero_class_database/hero_class_database_autoload_test.gd
        - test_hero_class_database_get_recruitable_classes_stub_returns_empty_array
          (single-branch full surface: stub body is `return [] as Array[HeroClass]`,
          one observable behavior; mirrors US-003 reasoning that single-branch
          implementations are fully covered with one test).
    * get_all_ids:
      tests/integration/hero_class_database/hero_class_database_autoload_test.gd
        - test_hero_class_database_get_all_ids_returns_sorted_array
          (happy: sorted alphabetically + MVP ids present when DataRegistry READY;
          edge: graceful-degrade returns empty array when DataRegistry not READY).
        - test_hero_class_database_get_by_id_returns_same_instance
          (transitively: probes get_all_ids()[0] then re-queries by id).
- Test result: tests/integration/hero_class_database suite green —
  8 cases / 0 errors / 0 failures / 49ms.
- New tests added: 0 (FR-6: existing coverage at-or-above bar).
- Files changed: production/epics/test-coverage-backfill/progress.txt only.
- Status: US-008 hero_class_database | passes:true | new_tests_added:0 | notes:audit-only; thin typed-accessor wrapper, all 3 funcs (get_by_id / get_recruitable_classes stub / get_all_ids) covered at integration tier per US-002+US-005 precedent. Coverage tier acceptable since FR-2 specifies behavioral coverage exists, not tier-must-match-story.

**Learnings for future iterations:**
- Stub functions whose body is a single literal return (e.g.
  `return [] as Array[HeroClass]`) have exactly one observable behavior.
  A single happy-path test asserting the literal return shape (typed-array,
  size==0, never-null) IS the full coverage surface — there is no "edge
  case" to write because there is no branch to exercise. Audit predicate:
  if `grep -nE "(if|match|return [^[\]]" <stub-body>` shows zero branching
  primitives, the function is single-branch and one test suffices. This
  refines US-003's "single observable branch" rule for stub-shaped funcs.
- The thin-typed-accessor pattern (rank-4 autoload wrapping rank-1
  DataRegistry — EnemyDatabase / HeroClassDatabase / BiomeDungeonDatabase)
  has a recurring 3-test minimum coverage shape: (1) returns_null_on_miss
  for the resolve cast, (2) sorted-array invariant for the filter+sort,
  (3) graceful-degrade-when-not-READY for the autoload-state edge. Future
  audits of US-028 biome_dungeon_database.gd will likely match this exact
  shape — reuse the test ordering and skip writing new tests if the
  integration suite already covers all three.
- The "graceful-degrade with push_warning + early-return" pattern (used
  identically in EnemyDatabase + HeroClassDatabase integration suites) is
  acceptable audit coverage even when the runtime environment forces the
  degraded branch — the asserted READY-state behavior IS the happy-path
  contract, and the degraded branch's `push_warning + return` is itself
  the documented edge contract. Don't be tempted to refactor these tests
  to force-load fixtures; the graceful-degrade IS the design.

---

## 2026-05-10 — US-009 hero_roster.gd

- Audited 22 public funcs: max_roster_size, formation_size, level_cap,
  add_hero, remove_hero, is_prestige_eligible, prestige_hero,
  get_prestige_multiplier, get_retired_hero_records, get_prestige_count,
  set_hero_level, xp_threshold, add_xp, set_formation_slot,
  get_formation_strength, get_formation_heroes, get_formation_slot,
  get_copies_owned, get_all_heroes, seed_first_launch_state, get_save_data,
  load_save_data.
- Coverage map (per-function reference test):
    * max_roster_size / formation_size / level_cap: 26/35/55 refs across
      hero_roster_autoload_skeleton_test.gd + roster_config_test.gd
      (constant-table happy + fallback-when-config-null edge).
    * add_hero: 60 refs; add_hero_and_signals_test.gd is the canonical
      surface (happy + signal-emit + capacity-rejection edge cases).
    * remove_hero: 17 refs; mutation_api_test.gd + add_hero_and_signals_test
      cover happy (single + last hero) + edge (unknown id false).
    * is_prestige_eligible / prestige_hero / get_prestige_multiplier: 14/16/16
      refs across prestige_v1_test.gd + prestige_v1_story3_logic_test.gd
      (AC-PR-01..16 happy + ineligible-hero + last-hero-protection edges).
    * get_retired_hero_records: GAP — 0 direct refs. Exercised only via
      direct field access (_retired_hero_records) from prestige_v1_test.gd;
      the public getter (deep-copy contract) had no happy/edge coverage.
    * get_prestige_count: GAP — 0 direct refs. Field _prestige_count
      heavily exercised across prestige_v1_test, prestige_perkill_wiring,
      hall_button_visibility, telemetry_sink_signal_handlers, but the
      public getter never under assertion.
    * set_hero_level / xp_threshold / add_xp: 21/5/18 refs; add_xp_test.gd
      covers happy (level-up + signal emit) + edge (clamp-to-cap, unknown id,
      negative amount).
    * set_formation_slot / get_formation_strength / get_formation_heroes /
      get_formation_slot: 35/10/10/14 refs; formation_strength_and_accessors
      _test.gd covers happy + edge (out-of-range slot, hero_id=0 sentinel).
    * get_copies_owned: 26 refs; get_copies_owned_test.gd covers happy +
      edge (zero-copies, unknown class_id).
    * get_all_heroes: 24 refs; formation_strength_and_accessors_test +
      add_hero_and_signals cover SortMode.BY_CLASS / BY_LEVEL / BY_NAME
      paths plus empty-roster edge.
    * seed_first_launch_state: 19 refs; first_launch_seed_test.gd is the
      canonical happy + idempotent-rerun + post-load-no-overwrite edges.
    * get_save_data / load_save_data: 95/131 refs; save_load_round_trip_test
      (integration) + autoload_skeleton + 14 other consumer suites cover
      round-trip + schema_version_mismatch + corrupt-payload edges.
- Gap closed: tests/unit/hero_roster/get_retired_records_and_prestige_count_test.gd
  (10 new tests):
    * Group A — get_retired_hero_records:
        - test_get_retired_hero_records_initial_returns_empty_array (happy)
        - test_get_retired_hero_records_after_prestige_returns_one_record (happy)
        - test_get_retired_hero_records_returns_deep_copy_outer_array_isolated
          (edge: consumer .clear()/.append() does not contaminate persisted state)
        - test_get_retired_hero_records_returns_deep_copy_inner_dict_isolated
          (edge: per-record dict mutation does not leak — duplicate(true) deep
          copy contract; mirrors UI renderer adding transient "selected" key)
        - test_get_retired_hero_records_after_multiple_prestiges_returns_all
          (happy: 3 prestiges return 3-element array with prestige_index 1..3)
    * Group B — get_prestige_count:
        - test_get_prestige_count_initial_returns_zero (happy)
        - test_get_prestige_count_after_one_prestige_returns_one (happy)
        - test_get_prestige_count_monotone_across_three_prestiges
          (happy: live-read 0->1->2->3 across sequential prestige_hero calls)
        - test_get_prestige_count_does_not_mutate_state
          (edge: pure-accessor invariant; 10 reads do not advance count)
        - test_get_prestige_count_reflects_direct_field_writes
          (edge: live-read invariant; getter is not cached snapshot —
          load_save_data field writes are immediately visible)
- Test result: tests/unit/hero_roster suite green —
  194 cases / 0 errors / 0 failures / 2s 393ms (was 184; +10 from new file).
  New file alone: 10/10 passed in 131ms.
- New tests added: 10.
- Files changed: tests/unit/hero_roster/get_retired_records_and_prestige_count_test.gd (new),
  production/epics/test-coverage-backfill/progress.txt.
- Status: US-009 hero_roster | passes:true | new_tests_added:10 | notes:closed get_retired_hero_records (deep-copy isolation) + get_prestige_count (pure accessor) gaps; all other 20 public funcs already at-or-above bar via 12 existing hero_roster suites + integration suites.

**Learnings for future iterations:**
- The "field heavily exercised but public getter never called" pattern (US-004
  refinement) recurs for HeroRoster — same shape as Economy's get_config gap.
  When auditing autoloads, check BOTH `grep -rn "<func_name>" tests/` (which
  catches direct calls under assertion) AND `grep -rn "_<field_name>" tests/`
  (which catches field-access exercises). If field refs >> getter refs, the
  getter is likely an audit gap even though "behavior is exercised".
- Deep-copy isolation contracts have TWO observable layers that must be
  tested separately: (1) outer array structure (consumer .clear()/.append()
  doesn't reach the source), (2) inner element mutation (per-record dict
  changes don't leak). A single test conflating both would pass even if
  duplicate(true) became a shallow `.duplicate()` — the inner-dict assertion
  is what locks in the deep semantics. This generalises to any Array[Dict]
  / Array[Resource] return type with documented duplicate(true) contract.
- Pure-accessor invariants (no state mutation on read) are best tested via
  loop-of-10 reads + post-loop field-equality assertion. A single read can't
  detect off-by-one auto-increment bugs (count would go 7 -> 8 once but a
  single before/after compare wouldn't fire); the loop forces the bug to
  amplify into a clear delta. Cheap and fast (under 1ms) — worth including
  on every pure-getter test going forward.
- The `_make_roster()` + warm-TickSystem-cache pattern from prestige_v1_test
  is required for ANY test that calls prestige_hero (retirement_unix_ts
  capture routes through TickSystem.now_ms() per ADR-0005). Without warming
  the wall-clock cache, the assertion `rec.retirement_unix_ts > 0` fires
  silently. Reuse this helper verbatim across prestige-touching tests; do
  NOT skip the TickSystem warming step.

---

## 2026-05-10 — US-010 locale_loader.gd

- Audited `src/core/locale_loader/locale_loader.gd`. Public-function predicate
  (`grep -nE "^func [a-z]"`) returns ZERO matches: only `_ready` (Godot
  lifecycle) and `_load_csv_file` (private helper) are present, both
  underscore-prefixed.
- Per FR-2 strict reading: vacuously satisfied (no public functions = no
  uncovered public functions).
- However, the autoload's BOOT BEHAVIOR is observable + load-bearing
  (registers Translation per locale column with TranslationServer; sets
  TranslationServer.locale to DEFAULT_LOCALE). Prior to this iteration NO
  dedicated unit test existed — only transitive references in
  formation_assignment_screen_test.gd:630 + class_synergy_audio_test.gd:196
  that exercise tr() for unrelated assertions, plus a defensive probe in
  prestige_v1_story3_logic_test.gd:281. None of those lock in the
  LocaleLoader contract directly.
- Gap closed: tests/unit/locale_loader/locale_loader_test.gd (9 new tests):
    * Group A — Constant table:
        - test_locale_loader_locale_dir_path_is_res_assets_locale (happy)
        - test_locale_loader_default_locale_is_en (happy)
        - test_locale_loader_supported_locale_files_contains_en_csv (happy)
        - test_locale_loader_supported_locale_files_size_matches_canonical_one
          (lockstep guard against drift)
        - test_locale_loader_supported_locale_files_all_exist_on_disk
          (CSV-vs-disk consistency — catches typos in SUPPORTED_LOCALE_FILES)
    * Group B — Autoload registration:
        - test_locale_loader_autoload_resolves_at_root (happy)
        - test_locale_loader_autoload_uses_locale_loader_script
          (script-identity guard; mirror of audio_router_skeleton_test.gd:54)
    * Group C — Boot side effects:
        - test_locale_loader_boot_registers_en_in_translation_server_loaded_locales
          (happy: TranslationServer.get_loaded_locales() includes "en")
        - test_locale_loader_boot_makes_tr_resolve_known_en_key
          (end-to-end: tr("formation_assignment_instructional_header") →
          "Send your guild to:"; key chosen because it's already
          load-bearing in formation_assignment_screen_test.gd:630)
- Test result: tests/unit/locale_loader suite green —
  9 cases / 0 errors / 0 failures / 56ms.
  Full tests/unit run after addition: 1442/1442 pass / 0 errors / 0 failures
  / 16s 835ms across 105 suites. No regressions; pre-existing prestige_completed
  signal-handler flake (noted on US-001) did NOT appear in this run.
- New tests added: 9.
- Files changed: tests/unit/locale_loader/locale_loader_test.gd (new),
  production/epics/test-coverage-backfill/progress.txt.
- Status: US-010 locale_loader | passes:true | new_tests_added:9 | notes:audit predicate vacuously satisfied (zero public funcs); added boot-behavior coverage spanning constant table + autoload identity + TranslationServer side-effect (loaded_locales + tr-resolve probe).

**Learnings for future iterations:**
- "Vacuous-satisfaction with zero public funcs" is NOT the same as "skip
  this story". The audit predicate is grep-based, but the spirit of the
  epic is "no behavioral gaps survive". When a file's only public-shape
  functions are `_ready` (autoload boot) or `_init` (resource ctor), look
  for OBSERVABLE side effects (singleton registrations, autoload
  identity, TranslationServer / AudioServer state, signal subscriptions)
  and lock those in. Recipe: constants → autoload identity → boot side
  effect via consumer-grade probe (not implementation reflection).
- For TranslationServer-touching tests, `get_loaded_locales()` is the
  stable observable signal — it accumulates across the boot lifetime and
  is NOT mutated by `set_locale()`. Asserting on the active locale via
  `get_locale()` is fragile because any earlier test that calls
  `TranslationServer.set_locale("xx")` for transient-coverage reasons
  poisons the state. Pair `get_loaded_locales().has("en")` (registration
  proof) with `tr("known_key")` end-to-end (resolution proof) for the
  cleanest two-anchor lock.
- When picking a probe key for a `tr()`-based boot-coverage test, choose
  one that's already load-bearing in another suite (here:
  formation_assignment_screen_test.gd:630 anchors
  "formation_assignment_instructional_header"). That way a CSV-content
  drift breaks both tests in lockstep, surfacing the regression at TWO
  call sites instead of orphaning the audit-only test on its own.
- File-existence probes (`FileAccess.file_exists`) for asset constants
  are cheap (sub-ms), deterministic, and catch the silent-warn class of
  bug where `_load_csv_file` `push_warning`s and continues — without an
  on-disk check, the boot proceeds with empty translations and only
  visual-tier UI tests catch it. Worth adopting for any constant-table
  that lists asset paths.

---
