# Smoke Check Report — Sprint 4

**Date**: 2026-04-16
**Sprint**: Sprint 4 — Combo-Synergy Foundation
**Engine**: Unity 6000.3.11f1 (Unity 6.3 LTS)
**QA Plan**: `production/qa/qa-plan-sprint-04-2026-04-15.md`
**Argument**: `sprint`

---

## Automated Tests

**Status**: PASS (user-confirmed — all Sprint 4 tests pass in Unity Test Runner, no regressions from Sprint 1-3)

Unity tests cannot be run headlessly from shell; user confirmation accepted per `/smoke-check` NOT RUN protocol. User ran Test Runner locally and confirmed all pass.

**Test count delivered this sprint**:

| Story | Test file(s) | Test count |
|---|---|---|
| E4-001 Extend ComboDefinition | `Assets/Trizzle/Tests/Combo/ComboDefinitionSchemaTest.cs` | Schema validation suite |
| E4-002 ComboEffect Base Class | `Assets/Trizzle/Tests/Combo/ComboRegistryTest.cs` | Base class + registry (QA plan's 2 files merged into 1) |
| E4-003 Mage Combo Effects (5) | `Assets/Trizzle/Tests/Combo/MageEffects/` (5 files) | 30 tests (user-confirmed on E4-003 closure) |
| E4-008 Discovery Persistence | `ComboDiscoveryStoreTest.cs` + `ComboDiscoveryTrackerTest.cs` | 21 tests (pure store + PlayerPrefs + end-to-end event) |

---

## Test Coverage

| Story | Type | Test File | Coverage |
|---|---|---|---|
| E4-001 Extend ComboDefinition | Logic | `Assets/Trizzle/Tests/Combo/ComboDefinitionSchemaTest.cs` | COVERED |
| E4-002 ComboEffect Base Class | Logic | `Assets/Trizzle/Tests/Combo/ComboRegistryTest.cs` | COVERED |
| E4-003 Mage Combo Effects (5) | Logic | `Assets/Trizzle/Tests/Combo/MageEffects/*.cs` (5 files) | COVERED |
| E4-008 Discovery Persistence | Logic + Integration | `Assets/Trizzle/Tests/Combo/ComboDiscoveryStoreTest.cs` + `ComboDiscoveryTrackerTest.cs` | COVERED |

**Summary**: 4 of 4 Must Have stories COVERED. 0 MISSING. 0 manual-only.

**Naming deviations from QA plan** (non-blocking, equivalent or superior coverage):

- E4-001 — `ComboDefinitionSchemaTest.cs` vs plan's `ComboDefinitionTest.cs`
- E4-002 — merged `ComboEffectBaseTest.cs` + `ComboRegistryTest.cs` into single `ComboRegistryTest.cs`
- E4-008 — split into `ComboDiscoveryStoreTest.cs` + `ComboDiscoveryTrackerTest.cs` (vs plan's single `DiscoveryPersistenceTest.cs`); 21 tests including end-to-end event-path via reflection-injected real `ComboRegistry`

---

## Manual Smoke Checks

- [x] Game launches without crash — **PASS**
- [x] New game starts — **PASS**
- [x] Menus respond to input — **PASS**
- [—] Existing 5 Mage combos appear in draft — **N/A** (scene-attach deferred; see Deferred Integration below)
- [—] Inferno combo spawns burning ground patch — **N/A** (scene-attach deferred)
- [—] Quit-reload preserves discoveries — **N/A** (scene-attach deferred)
- [x] Sprint 3 features (combat, boss, difficulty) — no regressions — **PASS** (user-confirmed via Test Runner)

---

## Missing Test Evidence

All Logic and Integration stories have test coverage. No MISSING entries.

---

## Deferred Integration (carried condition for `/team-qa`)

**Scene-wiring pending (PR #118 followup)**: `ComboRegistry` + `ComboDiscoveryTracker` MonoBehaviours are NOT yet attached to the game scene root. Their Inspector references (`_database`, `_comboRegistry`, etc.) remain unassigned. Consequences:

- Combo effects cannot fire in Play mode
- Combo discovery cannot persist to PlayerPrefs in live gameplay
- Manual verification of Sprint 4 combo behavior in-game is not possible this sprint

**What is verified**:

- All Sprint 4 code paths are covered by unit + integration tests
- `ComboDiscoveryTrackerTest.Tracker_OnComboDiscoveredEvent_PersistsToPlayerPrefs` uses reflection to inject a real `ComboRegistry` MonoBehaviour and fire `OnComboDiscovered` end-to-end, verifying the event → store → PlayerPrefs flow at the code level
- Mage combo effects are unit-tested in isolation (30 tests across 5 files)

**What is deferred**:

- AoE side-effect playtest verification (Inferno patch tick, Thunderstrike bonus damage, Supernova explosion) — carried over from E4-003 closure
- In-game "discover a combo, quit, relaunch" end-to-end scenario (E4-008 AC-7 live verification)

**Recommendation for `/team-qa sprint-04`**: mark combo live-verification as CONDITIONAL in sign-off, pending the PR #118 scene-attach work. Matches the Sprint 3 pattern where E3-004 Ground Slam playtest was a carried condition.

---

## Verdict: **PASS WITH WARNINGS**

**Rationale**:

- Automated tests: PASS (user-confirmed)
- Core stability: PASS
- Regression check: PASS
- Test coverage: 4/4 COVERED (zero MISSING)
- **Warning**: Scene-attach deferral means combo behavior + discovery persistence are unit/integration tested but not live-verified in Play mode. This is a known followup (PR #118), not a Sprint 4 code gap.

Strict interpretation of `/smoke-check` skill rules would yield PASS (no automated failures, no Batch 1/2 FAIL, no MISSING test evidence). The PASS WITH WARNINGS verdict is used deliberately to flag the scene-attach deferral to `/team-qa` so it appears as an explicit carried condition in sign-off, rather than silently marking Sprint 4 APPROVED without caveat.

---

## Next Steps

1. Run `/team-qa sprint` to execute the QA plan and produce the sign-off report. Pass this smoke-check path for reference.
2. `/team-qa` should return APPROVED WITH CONDITIONS (scene-attach + AoE playtest carried forward).
3. After sign-off: `/gate-check` to advance to Sprint 5.
