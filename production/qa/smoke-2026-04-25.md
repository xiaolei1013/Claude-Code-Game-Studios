# Smoke Check — 2026-04-25 (Sprint 2 Must Have closure)

**Date**: 2026-04-25
**Operator**: Solo developer (autonomous run)
**Build**: Local dev (Godot 4.6.1 stable mono — `/Applications/Godot_mono.app/Contents/MacOS/Godot`)
**Sprint**: 2 (in progress; 7/7 Must Have stories implemented and tested)
**Verdict**: **PASS WITH NOTES**

---

## Scope

Per `production/qa/qa-plan-sprint-2-2026-04-25.md` §Smoke Test Scope:

1. ✅ Game launches headless without crash
2. ✅ Autoload registration: TickSystem(0), DataRegistry(1), Economy(rank 3 — note rank-2 hole), HeroClassDatabase(rank 4)
3. ⚠️ DataRegistry transitions to READY state with all 7 categories scanned — **NOT YET REACHABLE**: classes(3) ✓, config(1) ✓ are now met, but enemies(5)/biomes(1)/dungeons(1)/matchup(1) still empty. DataRegistry transitions to ERROR. **Expected for Sprint 2** — those categories' content lands in Sprint 3.
4. ✅ EconomyConfig resolvable (via direct ResourceLoader path; DataRegistry-mediated resolve also tested in `economy_config_schema_test.gd::test_economy_config_resolves_via_data_registry` and gracefully degrades when DataRegistry is in ERROR)
5. ✅ 3 MVP class .tres files resolvable (probe script `tests/probes/probe_class_tres.gd` confirms all 3 load via `ResourceLoader.load()`; L15 stat values verified against GDD §D.4 sanity table exactly)
6. ✅ No regression in Sprint 1 systems (TickSystem `tick_fired`, DataRegistry per-type validators) — all Sprint 1 economy + hero-class-database test suites pass
7. ✅ Performance: project import < 5 s; test suite < 250 ms wall total

---

## Probe Results — 3 MVP class .tres files

Ran `tests/probes/probe_class_tres.gd` (one-shot SceneTree script):

```
[OK] res://assets/data/classes/warrior.tres — id=warrior tier=1 role=tank archetype=bruiser L15_attack=40 L15_hp=358 L15_speed=20
[OK] res://assets/data/classes/mage.tres    — id=mage    tier=1 role=striker archetype=caster L15_attack=62 L15_hp=210 L15_speed=24
[OK] res://assets/data/classes/rogue.tres   — id=rogue   tier=1 role=precision archetype=armored L15_attack=42 L15_hp=167 L15_speed=44
[PASS] All 3 MVP class .tres files loadable
```

All L15 derived stats match the GDD §D.4 sanity table verbatim (Warrior 40/358/20, Mage 62/210/24, Rogue 42/167/44). Stat scaling formula `base + per_level × (level - 1)` will be unit-tested in S2-S2 (Should Have, may slip to Sprint 3).

Role values (`tank`/`striker`/`precision`) align with `design/registry/entities.yaml` canonical values — corrected from the story's draft suggestions (`frontline`/`ranged_dps`/`flanker`) to match the prior-art registry.

---

## Test Suite Results

Aggregate run via `addons/gdUnit4/bin/GdUnitCmdTool.gd --add res://tests/integration/hero_class_database/ --add res://tests/unit/hero_class_database/ --add res://tests/unit/economy/ --ignoreHeadlessMode`:

| Suite | Tests | Pass | Fail |
|---|---|---|---|
| `economy_autoload_skeleton_test.gd` (S2-M1) | 21 | 21 | 0 |
| `economy_config_schema_test.gd` (S2-M2) | 20 | 20 | 0 |
| `economy_add_gold_test.gd` (S2-M3) | 12 | 12 | 0 |
| `economy_try_spend_test.gd` (S2-M4) | 13 | 13 | 0 |
| `hero_class_resource_test.gd` (S2-M5) | 8 | 8 | 0 |
| `hero_class_database_autoload_test.gd` (S2-M6) | 8 | 8 | 0 |
| **Total** | **82** | **82** | **0** |

Sprint 1 boot-scan regression test (`test_boot_scan_load_order_matches_ordered_categories`) verified individually after the Sprint 2 ORDERED_CATEGORIES extension to include `"config"`: **7/7 PASSED**.

---

## Known shutdown SIGSEGV

Per Sprint 1 commit `3bc8c22` ("tolerate Godot shutdown SIGABRT by verifying test results via JUnit XML"): cumulative full-suite runs hit signal 11 at engine teardown after tests complete. Per-directory invocations (used here) are clean. Test results are written to `reports/report_*/results.xml` BEFORE the crash, so the crash is informational, not a test failure. Acknowledged.

---

## Tech debt notes

- **TD-006** (new, LOW severity) — DataRegistry stays in ERROR state during Sprint 2 testing because content categories (enemies/biomes/dungeons/matchup) lack `.tres` files. Cross-system tests that exercise `DataRegistry.resolve` via the live autoload (`test_economy_config_resolves_via_data_registry`, the 5 deferred tests in `hero_class_database_autoload_test.gd`) gracefully degrade with `push_warning + return`. They will assert real data once Sprint 3 adds enemy/biome content. Not a code defect — a content-volume gap by design.
- **TD-005** (existing, LOW) — `tests/gdunit4_runner.gd` still broken; CI uses `addons/gdUnit4/bin/GdUnitCmdTool.gd` directly. Unchanged from Sprint 1.

---

## Sign-off

- **Smoke check verdict**: PASS WITH NOTES (DataRegistry-ERROR state during Sprint 2 is expected — see scope item 3)
- **Reviewer**: Solo developer (`production/review-mode.txt = solo`)
- **Date**: 2026-04-25
